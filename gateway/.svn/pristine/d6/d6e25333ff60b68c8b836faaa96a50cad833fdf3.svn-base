<md-card>
<md-card-content>
<div class="row">
	<div class="col-md-8 narrowpad">
		<ol class="breadcrumb">
			<li class="breadcrumb-item"><a href="#">Setups</a></li>
			<li class="breadcrumb-item"><a href="#">File templates</a></li>
			<li class="breadcrumb-item active">{{currentstate}}</li>
		</ol>
	</div>
	<div class="col-md-4">
	
	</div>
		<!-- <i class="fa fa-plus fa-2x float-right"
			[routerLink]="['/', { outlets: { popup: ['file-templates-new'] } }]"
			placement="left" ngbTooltip="new template">
		</i>
		<i class="fa fa-pencil float-right"  style="font-size:24px" aria-hidden="true" 
		[routerLink]="['/', { outlets: { popup: ['file-templates-new'] } }]"
		ngbTooltip="edit"></i>
	    <i
			class="fa fa-plus fa-2x float-right" (click)="newTemplate()"
			placement="left" ngbTooltip="New Template" *ngIf="!tempdet"
			[hidden]="!templist"></i> <i class="fa save fa-2x  float-right"
			*ngIf="tempnew" (click)="templateSave()" placement="left"
			ngbTooltip="Save Template" [hidden]="templist">
	    </i> 
	    <i
			class="fa cancel fa-2x float-right" *ngIf="tempnew"
			(click)="newTemplatecancel()" placement="left" ngbTooltip="Cancel"
			[hidden]="templist">
		</i> -->

	


</div>
</md-card-content>
</md-card>