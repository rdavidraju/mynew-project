<div class="sub-side-navbar" style="margin-top: 78px;">
	<perfect-scrollbar style="height: auto; max-height:500px; width:100%; overflow-x: auto;">
		<h3 style="word-wrap:no-wrap;">test</h3>
		<!-- <ul >
		<li *ngFor="let connectionlist of sideBarData; let i = index">
			<div class="ripple psproviders">
				<i class="fa fa-caret-right hand ripple" aria-hidden="true"
					(click)="expandsource(connectionlist.connectionType)"
					*ngIf="sourcebody != connectionlist.connectionType"
					style="top: 3px;"></i> 
				<i class="fa fa-sort-desc hand ripple" aria-hidden="true"
					(click)="expandsource(connectionlist.connectionType)"
					*ngIf="sourcebody == connectionlist.connectionType"></i> 
					<img src="{{connectionlist.imagePath}}" class="filetemplateimgsource hand ripple" 
						(click)="expandsource(connectionlist.connectionType)"/>
					<span class="hand" (click)="expandsource(connectionlist.connectionType)">{{connectionlist.dispname}} - ({{connectionlist.count}})</span>
			</div>
			<div class="sourcebody"
				style="padding-left: 30px; word-wrap: no-wrap;"
				*ngIf="sourcebody == connectionlist.connectionType">
				<li  *ngFor="let connection of connectionlist.Lists; let i = index" routerLinkActive="router-link-active">
					<div class="selecteditem hand ripple" [routerLink]="['/source-connection-details', {outlets: {'content': [connection.id]+'/details'}}]">
						{{connection.name}}
					</div>
				</li>
			</div>
		</li>
	</ul> -->
	</perfect-scrollbar>
</div>